# mission-control-public

Curated, public-safe snapshot of what I'm building — generated from my private project journals by a curation pass. Consumed by my website's portfolio page.

- `portfolio.json` — the data. Schema is frozen at `version: 1`; fields are only ever added, never changed or removed without a version bump.

Raw URL:

```
https://raw.githubusercontent.com/bharat-patodi/mission-control-public/main/portfolio.json
```

This file is generated — don't edit it by hand.
